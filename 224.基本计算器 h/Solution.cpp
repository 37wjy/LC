#include <iostream>
#include <stack>


using  namespace std;

class Solution {
public:
    int calculate(string s) {
        int cnt = 0;
        for (uint64_t i = 0; i < s.size(); i++)
        {
            if (s[i]==' ')
            {
                continue;
            }
            if (/* condition */)
            {
                /* code */
            }
            
            INT64_MAX
        }
        return cnt;
        
    }
};